package Billing.billing;

/** Structural mirror of {@code Producer.model.StorageKind}, same "own copy, no shared class"
 *  convention {@link PlantType} already follows for {@code Producer.model.PlantType}. */
public enum StorageKind {
    BATTERY,
    HYDROGEN
}
